import { View, Text, StyleSheet } from 'react-native';

export default function DetailsScreenQ() {
  return (
    <View style={styles.container}>
      <Text>Details screen Q User</Text>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
});
